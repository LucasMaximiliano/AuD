/* PSEUDOCODE:
 * g_tailrec(x,y)
 *      if x<10
 *          return pow(x,y)
 *      else
 *          return sum = helper(x/10,y+1)
 * 
 * helper(x,y,sum)
 *      return pow(x%10,y) + sum
 */

#include <stdio.h>
#include <math.h>

//...

int main()
{
    
    return 0;
}